{% extends 'base.html.twig' %}

{% block title %}MàJ API!{% endblock %}

{% block body %}
<div>
    <h1>Voici les gentils ✅</h1>

<div class="container-fluid">
    <div class="row index">
        {% for gentil in gentils %}
            <div class="col-lg-4 text-white hero">
                <span> {{ gentil.name }} - {{ gentil.publisher }} </span>
            </div>
        {% else %}
            <p><em>la table est vide</em></p>
        {% endfor %}
    </div>
</div>

</div>
{% endblock %}
